<Window x:Class="Stocks.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Stocks.WPF"
        xmlns:controls="clr-namespace:Stocks.WPF.Controls"
        xmlns:viewmodels="clr-namespace:Stocks.WPF.ViewModels"
        
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="900">

    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <controls:NavigationBar Grid.Row="0" DataContext="{Binding Navigator}" Grid.ColumnSpan="2" Margin="0,0,0,487" Grid.RowSpan="2"/>

        <ContentControl Grid.Row="1" Content="{Binding Navigator.CurrentViewModel}" Grid.ColumnSpan="2"/>

    </Grid>
</Window>
